/**
 * Local Expo module: native capture / DSP for SnoozePulse (ADR-16).
 *
 * Task 5.1: lifecycle + heartbeat stub only.
 * Task 5.2: AudioRecord / AVAudioEngine + real detection.
 */

export { default } from './src/SnoozePulseAudioModule';
export * from './src/SnoozePulseAudio.types';
