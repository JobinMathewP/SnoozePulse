import { SettingsScreen } from '@/features/settings';

/** Settings route — pushed from the Home header gear. Delegates to the feature screen. */
export default function SettingsRoute() {
  return <SettingsScreen />;
}
